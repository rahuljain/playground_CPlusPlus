#include<iostream.h>
#include<conio.h>
#include<stdio.h>

struct X
{
int Rno;
char name[20];
X prev ;
X next;
};

class double
{
X start;
X()
{
start=NULL;
}
};

j->prev=NULL;
j->next=p;
p->prev=j;
p->next=NULL;

//For inserting a node;

j->next->prev=q;
q->next=j->next;
q->prev=j;
j->next=q;


void main()
{
clrscr();

getch();
}